import React from "react";
import { useState } from "react";

const Functions = () => {
  const FuncX = (someThing) => {
    console.log(someThing);
  };
  FuncX();

  return <div>Functions</div>;
};

export default Functions;
